"""
SDXL LoRA Training Config Template for Kohya sd-scripts

Generates TOML configuration files for sdxl_train_network.py
"""

import os


def generate_sdxl_training_config(
    name: str,
    image_folder: str,
    output_folder: str,
    model_path: str,
    steps: int = 500,
    learning_rate: float = 0.0001,
    lora_rank: int = 8,
    lora_alpha: int = 4,
    resolution: int = 1024,
    batch_size: int = 1,
    optimizer: str = "AdamW8bit",
    mixed_precision: str = "fp16",
    gradient_checkpointing: bool = True,
    cache_latents: bool = True,
    cache_text_encoder_outputs: bool = True,
) -> str:
    """
    Generate a TOML config file for SDXL LoRA training with sd-scripts.

    Returns the config as a TOML string.
    """

    # Escape backslashes for TOML on Windows
    model_path_escaped = model_path.replace('\\', '/')
    image_folder_escaped = image_folder.replace('\\', '/')
    output_folder_escaped = output_folder.replace('\\', '/')

    config = f'''# SDXL LoRA Training Config
# Generated by ComfyUI SDXL LoRA Trainer

[general]
enable_bucket = true
bucket_no_upscale = true

[model]
pretrained_model_name_or_path = "{model_path_escaped}"

[dataset]
train_data_dir = "{image_folder_escaped}"
resolution = "{resolution},{resolution}"
caption_extension = ".txt"

[network]
network_module = "networks.lora"
network_dim = {lora_rank}
network_alpha = {lora_alpha}
network_train_unet_only = true

[optimizer]
optimizer_type = "{optimizer}"
learning_rate = {learning_rate}
lr_scheduler = "constant"

[training]
output_dir = "{output_folder_escaped}"
output_name = "{name}"
save_model_as = "safetensors"
save_precision = "fp16"
max_train_steps = {steps}
train_batch_size = {batch_size}
mixed_precision = "{mixed_precision}"
gradient_checkpointing = {str(gradient_checkpointing).lower()}
cache_latents = {str(cache_latents).lower()}
cache_text_encoder_outputs = {str(cache_text_encoder_outputs).lower()}
sdpa = true
max_data_loader_n_workers = 0
seed = 42
'''

    return config


def save_config(config_content: str, config_path: str):
    """Save config content to a TOML file."""
    with open(config_path, 'w', encoding='utf-8') as f:
        f.write(config_content)


# VRAM mode presets for SDXL (rank is user-controlled, not preset)
SDXL_VRAM_PRESETS = {
    "Min (512px)": {
        "optimizer": "AdamW8bit",
        "mixed_precision": "fp16",
        "batch_size": 1,
        "gradient_checkpointing": True,
        "cache_latents": True,
        "cache_text_encoder_outputs": True,
        "resolution": 512,
    },
    "Low (768px)": {
        "optimizer": "AdamW8bit",
        "mixed_precision": "fp16",
        "batch_size": 1,
        "gradient_checkpointing": True,
        "cache_latents": True,
        "cache_text_encoder_outputs": True,
        "resolution": 768,
    },
    "Max (1024px)": {
        "optimizer": "AdamW",
        "mixed_precision": "bf16",
        "batch_size": 4,
        "gradient_checkpointing": False,
        "cache_latents": True,
        "cache_text_encoder_outputs": False,
        "resolution": 1024,
    },
}
